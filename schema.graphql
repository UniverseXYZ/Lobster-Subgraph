type TransferEntity @entity {
  id: ID!
  from: Bytes!
  to: Bytes!
  tokenId: BigInt!
  tokenURI: String!
  gene: BigInt!
}

type MintedEntity @entity {
  id: ID!
  to: Bytes!
  tokenId: BigInt!
  tokenURI: String!
}

type Trait @entity {
  id: ID!
  count: BigInt
  rarity: BigDecimal
}